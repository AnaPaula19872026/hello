import type { ReactNode } from 'react';
export function StatCard({title,value,icon,alert}:{title:string;value:string|number;icon?:ReactNode;alert?:boolean}){return <div className={`rounded-2xl border p-4 shadow-soft ${alert?'border-red-200 bg-red-50':'border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900'}`}><div className="flex items-center justify-between"><p className="text-sm font-semibold text-slate-500 dark:text-slate-400">{title}</p>{icon}</div><strong className={`mt-2 block text-3xl font-black ${alert?'text-red-700':'text-slate-900 dark:text-white'}`}>{value}</strong></div>}
